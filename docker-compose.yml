services:
  tkhib:
    build: .
    container_name: tkhib
    restart: unless-stopped
    env_file:
      - .env
    environment:
      TKHIB_DATA_DIR: /app/data
      HOST: 0.0.0.0
      PORT: 8080
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
